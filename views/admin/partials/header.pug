header.header
	.inner-wrap
		.inner-button-menu
			<i class="fa-solid fa-indent"></i>
		.inner-logo
			a(href=`/${pathAdmin}/dashboard`)
				span HP
				span Exam
	.inner-account
		.inner-text
			.inner-name #{currentUser ? `${currentUser.ho || ""} ${currentUser.ten || currentUser.username || ""}` : "Chưa đăng nhập"}
			.inner-role #{currentUser ? currentUser.role : ""}
		

